# Set default behavior for all files
* text=auto

# Normalize text files to ensure consistent line endings across platforms
*.html text
*.css text
*.js text
*.json text
*.php text
*.sql text
*.scss text diff=scss
*.js text eol=lf
*.php text
*.jsx text
*.ts text
*.tsx text

# Binary files - mark them so that Git doesn't try to handle them as text
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
*.ai binary
*.psd binary

# Minified files should also be treated as binary to avoid diff issues
*.min.js binary
*.min.css binary

# Diff settings for specific file formats
*.yml text diff=yaml
*.config text diff=xml

# Prevent certain files from being included in archive exports
.gitattributes export-ignore
.gitignore export-ignore
README.md export-ignore

# Performance optimizations for Linguist
documentation/* linguist-generated=true
*.log linguist-detected=false

# Ensure certain custom settings for JavaScript files
*.js eol=lf
