# ──────────────────────────────────────────────────────────────────────────────
# Repo layout
# - App lives in /website
# - Docs live in /docs-vefhonnun
# - Build artifacts and local env files are ignored
# ──────────────────────────────────────────────────────────────────────────────

# App build artifacts
website/node_modules/
website/.next/
website/.vscode/
website/out/
website/dist/
website/coverage/

# Optional per-env local files (never commit secrets)
website/.env
website/.env.local
website/.env.production
website/.env.development
# If you keep envs at repo root instead, uncomment below:
#.env
#.env.local
#.env.production

# Prisma (optional: if you ever generate SQLite locally)
website/prisma/*.db
website/prisma/*.sqlite
website/prisma/*.sqlite-journal

# Production assets (decide if code or infra)
# If these are runtime assets the app needs at runtime:
#   move them under website/Production/ and keep tracked as needed.
# If they are deploy-only or machine local, ignore them:
website/Production/

# Logs & temp
*.log
*.log-*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*
*.tmp
*.temp
*.cache
.cache/
.tmp/

# OS junk
.DS_Store
Thumbs.db
Desktop.ini

# Editors / IDE
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.sublime-project
*.sublime-workspace

# Tooling / build caches
.next/
dist/
out/
coverage/
.eslintcache

# Node (root, in case you accidentally install here)
node_modules/

# Lockfiles — DO NOT IGNORE (keep them tracked for reproducible installs)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Misc archives/binaries
*.zip
*.tar
*.tar.gz
*.7z
*.rar
*.exe
*.dll
*.out
*.obj

# If you use GitHub Pages or static export (adjust as needed)
# docs/ or site build output:
docs/_build/

# If you ever add Python tooling locally (safe ignores)
__pycache__/
*.py[cod]
.venv/
venv/
